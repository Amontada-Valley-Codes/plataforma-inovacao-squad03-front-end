'use client'
import SubmissionForm from "@/components/elements/SubmissionPOC/SubmissionForm"

export default function Submission() {
    return (
        <div className="flex items-center justify-center py-4 w-full">
            <SubmissionForm/>
        </div>
    )
}

